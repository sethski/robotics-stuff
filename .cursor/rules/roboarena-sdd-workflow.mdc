---
description: RoboArena SDD workflow — task branches, granular commits, sequential PRs
alwaysApply: true
---

# RoboArena delivery workflow

## Branches

- Never commit product work on `master`.
- Keep a long-lived **integration branch** (e.g. `feat/part-system-foundation`).
- Every plan task / component slice gets its **own branch** off the current integration tip:
  - `feat/task-<N>-<slug>`
  - `feat/task-<N>a-<slug>` for a parallel slice
- Implementers commit only on their task branch, never on the integration branch.

## Granular commits

One logical change per commit. Typical task shape:

1. failing test (RED)
2. minimal implementation (GREEN)
3. polish only if needed

Do not squash a whole task into one commit unless the user asks.

Author **and** committer must be `Troy-LL <Troy-LL@users.noreply.github.com>` (GitHub rejects private committer emails).

Stamp every commit in **GMT+8 (`+0800`)** with `GIT_AUTHOR_DATE` / `GIT_COMMITTER_DATE` — this machine’s local TZ is Pacific and must not be used. Recipe in `.cursor/skills/roboarena-engineering/SKILL.md`.

## Parallel coding, sequential landing

- Parallel **coding** only on disjoint file sets (dependency DAG).
- Parallel **landing** is forbidden.
- After review-clean work on a task branch:

  1. `git push -u origin HEAD`
  2. `gh pr create` into the **integration branch** (not directly into `master`)
  3. Controller reviews the PR
  4. `gh pr merge` through that PR
  5. Promote: open/merge a PR from the **integration branch → `master`** so `master` stays current
  6. Only then start the next task merge

## Models

- **Orchestrator (this chat):** Grok — reviews, PRs, merges, dispatch.
- **Implementers / fixers:** composer 2.5 (`composer-2.5-fast`). Do not inherit the orchestrator model.

## Details

Full procedure: `.cursor/skills/roboarena-sdd/SKILL.md`  
Engineering constraints: `.cursor/skills/roboarena-engineering/SKILL.md`
